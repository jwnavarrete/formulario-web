const config = {
    host: process.env.HOST_API,
    port: process.env.PORT_API,
    basename: '/',
    defaultPath: '/home',
    fontFamily: 'Roboto, sans-serif',
    borderRadius: 12
  }
  
export default config
